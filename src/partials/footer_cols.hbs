<footer class="bg-white border-t">
  <div class="max-w-6xl mx-auto px-4 py-12 grid md:grid-cols-4 gap-8 text-sm">
    <div>
      <img src="{{../brand.logo}}" alt="{{../brand.name}}" class="h-6 w-auto">
      <p class="mt-3 text-slate-500">bello is a trading name of Bello Insurance Services LLC.</p>
    </div>
    {{#each data.columns}}
    <div>
      <h4 class="font-semibold mb-3">{{@key}}</h4>
      <ul class="space-y-2">
        {{#each this}}<li><a href="#" class="hover:underline">{{this}}</a></li>{{/each}}
      </ul>
    </div>
    {{/each}}
  </div>
  <div class="max-w-6xl mx-auto px-4 pb-6 flex items-center justify-between text-xs text-slate-500">
    <span>© 2025 bello insurance Services LLC. All rights reserved.</span>
    <div class="flex gap-4">
      {{#each data.social}}<a href="{{href}}" class="hover:underline">{{label}}</a>{{/each}}
    </div>
  </div>
</footer>
